import { createTheme } from '@mui/material/styles';


const theme = createTheme({
    palette: {
        primary: {
            main: "#faece5d4"
        },
        secondary: {
            main: "#fbd6c8c5"
        }
        }
})

export default theme;